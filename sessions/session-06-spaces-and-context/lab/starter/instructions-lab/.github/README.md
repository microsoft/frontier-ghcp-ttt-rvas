# .github directory

This directory starts empty. Create custom instruction files here during the lab.

Files to create:

- `copilot-instructions.md` — repository-level Copilot instructions
- `prompts/` — reusable prompt files
