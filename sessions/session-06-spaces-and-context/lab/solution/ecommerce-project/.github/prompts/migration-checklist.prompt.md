---
description: "Review a proposed dependency migration"
---

Create a migration checklist for `{{ proposed_change }}` in the Storefront API.

Cover affected files, breaking changes, focused tests, rollback steps, and the
approval needed before package changes. Stop before editing files or installing
packages.
