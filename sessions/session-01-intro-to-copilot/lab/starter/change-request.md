# Change request

Rename the first parameter of `profile_summary` from `username` to `handle`.

Use Next Edit Suggestions if the approved editor offers them. Review every proposed
follow-up edit. Search for `username` after the rename because a predicted edit can
miss a caller.
