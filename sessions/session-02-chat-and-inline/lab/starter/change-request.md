# Change request

The calculator must reject a weighted average when the total weight is zero.

The request does not define the exception type or message. Decide both before
editing code. Add a focused test that records the decision, then update
`weighted_average`.
