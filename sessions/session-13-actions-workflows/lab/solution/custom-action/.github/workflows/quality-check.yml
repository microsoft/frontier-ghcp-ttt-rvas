name: Quality check

on:
  workflow_dispatch:

jobs:
  quality-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci --prefix lab/solution/custom-action
      - uses: ./lab/solution/custom-action
        with:
          threshold: '0'
          report-format: markdown
