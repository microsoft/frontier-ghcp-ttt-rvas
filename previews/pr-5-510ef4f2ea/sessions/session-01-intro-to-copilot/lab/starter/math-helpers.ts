/**
 * Math Helper Functions (TypeScript)
 * ===================================
 * Complete each function using GitHub Copilot's inline suggestions.
 *
 * Notice how TypeScript type annotations give Copilot additional context,
 * often leading to more precise suggestions than plain JavaScript.
 */

/**
 * Clamp a value to a specified range.
 * If the value is below min, return min. If above max, return max.
 * Otherwise return the value unchanged.
 */
export function clamp(value: number, min: number, max: number): number {
  // Let Copilot suggest the implementation here
}

/**
 * Linear interpolation between two values.
 * When t=0, returns start. When t=1, returns end.
 * Values of t between 0 and 1 return proportional values between start and end.
 */
export function lerp(start: number, end: number, t: number): number {
  // Let Copilot suggest the implementation here
}

/**
 * Check if a number is prime.
 * A prime number is a natural number greater than 1 that has no positive
 * divisors other than 1 and itself.
 */
export function isPrime(n: number): boolean {
  // Let Copilot suggest the implementation here
}

/**
 * Calculate the factorial of a non-negative integer.
 * factorial(0) = 1, factorial(n) = n * factorial(n-1)
 * Throws an error for negative numbers.
 */
export function factorial(n: number): number {
  // Let Copilot suggest the implementation here
}
