SERVICE REQUEST PORTAL - SELECTED DIFF EVIDENCE
================================================

This file is a synthetic, plain-language extract prepared by the portal engineering
lead. It records relevant behavior from the proposed change without exposing source
code. It is evidence for training, not a complete code review.

[E1] Request data
- Adds an optional dueDate value to a request.
- Existing requests can omit dueDate.

[E2] Coordinator behavior
- Users with the coordinator role can add or change dueDate.
- The new control is labelled "Due date".
- The extract does not prove keyboard focus order or screen-reader output.

[E3] Requester behavior
- Requesters can see a formatted due date.
- The requester view has no due-date edit action.

[E4] Validation
- A date before the server's current calendar date is rejected.
- The extract does not show how time zones affect "current date".

[E5] Reminder behavior
- A reminder job is requested 24 hours before dueDate.
- The job is not requested when a request is already Resolved at scheduling time.
- The extract does not show what happens if a request becomes Resolved after the job
  is scheduled.
- The notification request uses a new event name:
  service-request.due-soon.v2

[E6] Scope
- No authentication or role-definition files changed.
- Reporting code did not change.
- Calendar integration and recurring reminders are absent.

[E7] Wording
- Requester text reads: "Target response date: <date>".
- The issue and stakeholder request use the term "due date".

